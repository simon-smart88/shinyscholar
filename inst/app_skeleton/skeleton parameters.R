#name of app library
app_library <- "disagApp"

#whether to include a map
include_map <- TRUE

#whether to include a table
include_table <- TRUE

#whether to include a code tab
include_code <- TRUE

common_objects <- list(
  ras = NULL,
  hist = NULL,
  scat = NULL,
  meta = NULL,
  poly = NULL,
  logger = NULL,
  state = NULL
)